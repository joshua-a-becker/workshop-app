// Template for server/src/secrets.js, which is gitignored. Copy this file to
// secrets.js and fill in the real values — the server will not start without it.
//
// DAILY_API_KEY: an API key for the Daily.co account that hosts the video rooms
// (domain `negotiate`). Found in the Daily dashboard under Developers.
export const DAILY_API_KEY = "";
